import 'package:flutter/material.dart';

class TColors {
  TColors._();

  // App Basic Colors
  static const Color primary = Color.fromRGBO(52, 146, 147, 1);

  // Text Colors
  static const Color textPrimary = Color.fromRGBO(0, 0, 0, 1);
  static const Color textSecondary = Color.fromRGBO(51, 51, 51, 1);
  static const Color textWhite = Color.fromRGBO(255, 255, 255, 1);

  // Background Colors
  static const Color background = Color.fromRGBO(250, 250, 250, 1);
  static const Color backgroundDark = Color.fromRGBO(30, 30, 30, 1);
  static const Color container = Color.fromRGBO(240, 240, 240, 1);

  // Button Colors
  static const Color buttonPrimary = primary;
  static const Color buttonSecondary = Color.fromRGBO(255, 255, 255, 1);
  static const Color buttonDisabled = Color.fromRGBO(100, 100, 100, 1);
  static const Color iconButtonPrimary = primary;
  static const Color iconButtonSecondary = Color.fromRGBO(50, 50, 50, 1);

  // Border Colors
  static const Color borderLight = Color.fromRGBO(220, 220, 220, 1);
  static const Color borderDark = Color.fromRGBO(100, 100, 100, 1);

  // Validation and State Colors
  static const Color error = Color.fromRGBO(220, 53, 69, 1);
  static const Color success = Color.fromRGBO(40, 167, 69, 1);
  static const Color warning = Color.fromRGBO(255, 193, 7, 1);
  static const Color info = Color.fromRGBO(23, 162, 184, 1);

  // Shadow Colors
  static const Color shadow = Color.fromRGBO(0, 0, 0, .5);

  // Neutral Colors
  static const Color white = Color.fromRGBO(255, 255, 255, 1);
  static const Color black = Color.fromRGBO(0, 0, 0, 1);
}